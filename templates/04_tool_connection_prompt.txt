자동화 설계 초안을 바탕으로 현재 작업공간의 `SKILL.md`를 먼저 검사해서 실제로 사용할 수 있는 스킬과 도구를 연결해줘.

[반드시 포함]
- 자동화 단계별 사용 도구
- 즉시 사용 가능 여부
- 추가 설정 필요 여부
- 그레이 영역 여부
- 예상 입력과 출력
- 판단 근거가 된 `SKILL.md` 또는 스크립트, 구체적인 연결 목적 및 수행 가능성, 부족함/보완점 등 언급

[규칙]
- 시작 전에 `python .claude\skills\_education_skills_중급\automation-pipeline-design-eduv-중급\scripts\find_gpters_study_docs.py --stage tool_connection` 결과를 확인
- `references/06_current-tool-catalog.md`를 우선 참고하고, 문서 매핑 결과의 `workflow_rules_template`, `workflow_rules_sample`, `student_guide`, `flow_reference`를 보조 참고로 사용
- 자동화 설계의 핵심 단계, 입력, 출력이 불명확하면 도구 연결보다 설계 보완이 우선
- `.claude/skills` 아래의 `SKILL.md`를 우선 탐색
- 필요하면 현재 작업공간 전체에서 `SKILL.md`를 추가 탐색
- 실제로 찾은 스킬만 사용 후보로 올리기
- 없는 기능은 있다고 가정하지 말고 그레이 영역으로 표시
- 복잡한 상태관리 도구는 중급 단계에서 우선 제외
- 사용자가 바로 이해할 수 있게 표 중심으로 정리
- AI 분석·생성·판단이 필요한 단계는 로컬 AI Agent(Claude Code, Codex CLI, Gemini CLI 등 현재 세션에서 실행 중인 AI)가 직접 수행하는 것으로 간주한다. 외부 API 키가 필요한 독립 호출 스크립트(예: anthropic SDK, openai SDK 직접 호출)를 도구로 제안하지 않으며 사용자가 요구하는 경우에만 제안함.

[출력]
1. 도구 연결표
2. 즉시 실행 가능한 단계
3. 추가 설정이 필요한 단계
4. 그레이 영역 제안
5. 도구 연결 전에 다시 설계 또는 인터뷰가 필요한 항목
